export const authMessages: { [key: string]: string } = {
  noRequestFound: 'No request found in GraphQL context',
  authorizationHeaderIsMissing: 'Authorization header is missing',
  invalidOrMissingToken: 'Invalid or missing token',
  tokenIsInvalidOrExpired: 'Token is invalid or expired',
};
